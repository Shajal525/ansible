---
- name: Setup after new install
  hosts: homeserver
  gather_facts: true
  become: yes

  roles:
    - wifi_setup
    - update_repo
    - create_sudo_user
    - generate_ssh_localhost
    - basics
