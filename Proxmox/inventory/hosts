[homeserver]
# Run createuser_playbook using this.
192.168.1.201 ansible_connection=ssh ansible_user="root" ansible_password="shajal"

# Run everything using this
# 192.168.1.202 ansible_connection=ssh ansible_user="{{ username }}" ansible_ssh_private_key_file="{{ ssh_key_path }}" ansible_ssh_port="{{ ssh_port }}"
