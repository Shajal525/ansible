[homeserver]
# 192.168.1.107 ansible_user="{{ username }}" ansible_connection=ssh ansible_ssh_private_key_file="{{ ssh_key_path }}" ansible_ssh_port="{{ ssh_port }}"
192.168.1.107 ansible_user="{{ username }}" ansible_password="{{ password }}"
